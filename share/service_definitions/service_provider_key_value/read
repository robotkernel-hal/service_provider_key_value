services:
  - name: service_provider_key_value/read
    request:
      - { name: keys, type: uint32_t, array: true }
    response:
      - { name: values, type: string, array: true }
      - { name: error_message, type: string, default: "no error" }
