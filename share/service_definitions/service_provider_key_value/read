services:
  service_provider_key_value/read:
    request:
      keys: { type: uint32_t, array: true }
    response:
      values: { type: string, array: true }
      error_message: { type: string, default: "no error" }
