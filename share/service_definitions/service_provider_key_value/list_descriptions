datatypes:
  service_provider_key_value/description:
    fields:  
      key: { type: uint32_t }
      description: { type: string }
      unit: { type: string }
      default_value: { type: string }
      format: { type: string }
      read_only: { type: uint8_t }
services:
  service_provider_key_value/list_descriptions:
    response:
      entries: { type: service_provider_key_value/description, array: true }
      error_message: { type: string, default: "no error" }
